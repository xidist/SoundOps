# Sample audio

Generate a small WAV file for an endpoint smoke test:

```powershell
python .\samples\generate_tone.py
```

The tone verifies the upload and inference path, but a real-world recording
such as music, speech, traffic, applause, or an animal sound will produce a
more interesting AudioSet classification.
