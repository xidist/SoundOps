apiVersion: v1
kind: Service
metadata:
  name: {{ include "soundops.fullname" . }}
  labels:
    {{- include "soundops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "soundops.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
